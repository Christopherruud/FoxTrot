/**
 * Initialize variables and updates database. 
 * All functions and variables are declared here. They are 
 * used/called from the intex.html file (making)
 */

function Course(domain){
	var domain;
	this.domain = domain;
	var level, id;
	var modules =[];
	var nextCourse;
}

function Module(courseId){
	var parentId;
	parentId = courseId;
	var test = [];
	var moduleName;
	var moduleId;
}

function Test(moduleId){
	var testId;
	testId = moduleId;
	var question;
	var answer;
}

